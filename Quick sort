/*
input:
6
6 5 4 3 2 1
output:
 1 2 3 4 5 6
time:7.008s
*/
       

#include <stdio.h>
int a[100000];
int n;
void quick_sort(int *a,int l,int r)
{
    if(l>=r)  return;
    int m=a[l],i=l,j=r;
    while(i<j)
    {
        while(a[i]<m) i++;
        while(a[j]>m) j--;
        if(i<j)
        {
            int t=a[i];
            a[i]=a[j];
            a[j]=t;
        }
    }
    quick_sort(a,l,i-1);
    quick_sort(a,j+1,r);
}

int main(){
    scanf("%d",&n);
    int i=0;
    for(i=0;i<n;i++)
    scanf("%d",a+i);
    quick_sort(a,0,n-1);
    for(i=0;i<n;i++)
      printf("% d",a[i]);
    return 0; 
}
